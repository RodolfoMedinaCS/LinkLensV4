export type Folder = {
    id: string;
    name: string;
    linkCount: number;
    summary: string;
};

export type Cluster = {
    id: string;
    name: string;
    linkCount: number;
    confidenceScore: number;
    summary: string;
}; 